import 'package:flutter/material.dart';
import '../types/widgets.dart';

class Categories extends WidgetWithTitle {
  const Categories({super.key}) : super(title: "Add");

  @override
  Widget build(BuildContext context) {
    return const Text("Add");
  }
}
